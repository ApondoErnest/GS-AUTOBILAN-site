<div class="gs-admin-sidebar-footer" x-show="$store.sidebar.isOpen">
    <span class="gs-admin-sidebar-footer__icon" aria-hidden="true">
        <x-heroicon-o-lock-closed />
    </span>

    <span>
        <span>{{ __('admin_chrome.sidebar.footer_label') }}</span>
        <strong>{{ __('admin_chrome.sidebar.footer_value') }}</strong>
    </span>
</div>
