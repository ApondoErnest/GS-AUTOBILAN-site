# OS
.DS_Store
Thumbs.db

# Env / secrets (never commit)
.env
.env.*
!.env.example

# Dependencies (Laravel / Node — populated in Phase 4+)
/vendor
/node_modules

# Laravel runtime
/public/hot
/public/storage
/storage/*.key
/storage/pail
/storage/framework/cache/data
/storage/framework/sessions
/storage/framework/views
/storage/logs
/bootstrap/cache/*.php
.phpunit.result.cache
.phpunit.cache
Homestead.json
Homestead.yaml
auth.json

# Build / IDE
/public/build
npm-debug.log
yarn-error.log
.idea
.vscode
*.swp
*.swo

# Cursor local (keep project rules if added later under .cursor/rules)
.cursor/plans
.cursor/*.log
